# ZMK Custom Config Build Workflow (究極の最終確定版 - ZMK 公式のワークスペース初期化)

name: Build ZMK Firmware (Ultimate Stable Build - Final Pattern)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    env:
      BOARD: seeeduino_xiao_ble
      KCONFIG_NONINTERACTIVE: 1
      # ZMK_DIR は廃止。ZMKリポジトリは West によって自動的に適切な場所にダウンロードされる。

    steps:
    - name: 1. Checkout ZMK Config
      uses: actions/checkout@v4
      with:
        submodules: true
        fetch-depth: 0

    - name: 2. Install West Tool
      run: pip install west

    - name: 3. Initialize West Workspace and Download Dependencies
      run: |
        echo "--- ZMKワークスペースを構築 ---"
        
        # 🚨 究極の最終修正: 現在のリポジトリのルートをWestワークスペースとして初期化
        # ZMKリポジトリをマニフェストとして指定し、現在のディレクトリにダウンロードさせる
        west init -m https://github.com/zmkfirmware/zmk --mr main
        
        # ワークスペースの依存関係を更新（ZMKリポジトリがダウンロードされる）
        west update

    - name: 4. Install Python Dependencies and Export Zephyr Base
      run: |
        echo "--- Python依存関係のインストールとZEPHYR_BASEの設定 ---"
        
        # ZMKリポジトリは 'zmk' または 'modules/zmk' にダウンロードされると想定されるため、
        # そのパスを正確に取得する (通常は 'zmk' にダウンロードされる)
        # 🚨 究極の最終修正: ZMKリポジトリのルートを特定
        ZMK_ROOT=$(west topdir) 
        
        # zephyr/scripts/requirements.txt を ZMK_ROOT から参照
        pip install -r "${ZMK_ROOT}/zephyr/scripts/requirements.txt"
        
        # ZEPHYR_BASE のパスを絶対パスで設定
        echo "ZEPHYR_BASE=${ZMK_ROOT}/zephyr" >> $GITHUB_ENV
        
        # west zephyr-export はワークスペースのルートから実行
        west zephyr-export | grep -E "^export " | sed -E 's/^export\s+//' >> $GITHUB_ENV

    - name: 5. Install Zephyr SDK
      run: |
        echo "--- Zephyr SDKのセットアップ ---"
        # SDKの明示的なインストールとパス設定 (v0.16.5)
        wget https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v0.16.5/zephyr-sdk-0.16.5_linux-x86_64.tar.xz -O /tmp/zephyr-sdk.tar.xz
        
        SDK_ROOT="${{ runner.tool_cache }}/zephyr-sdk-0.16.5"
        mkdir -p ${SDK_ROOT}
        
        # SDK展開のファイル名修正済み
        tar xf /tmp/zephyr-sdk.tar.xz -C ${SDK_ROOT} --strip-components=1
        
        # ZEPHYR_SDK_INSTALL_DIR のみを環境変数に設定
        echo "ZEPHYR_SDK_INSTALL_DIR=${SDK_ROOT}" >> $GITHUB_ENV

    - name: 6. Run West Build (ビルド実行)
      run: |
        echo "--- Zephyr環境の最終セットアップとビルドの実行 ---"
        
        # ビルドはワークスペースのルート（現在のリポジトリのルート）から実行
        # ZMKの公式推奨パターンでは、west build は現在のワークスペース全体をビルド対象とするため、-s は不要か '.' となる
        # 🚨 究極の最終修正: ZMKリポジトリ自体がビルドソースとなるため、'zmk' を指定
        west build -s zmk -d build -b ${{ env.BOARD }} -- \
          -DZMK_CONFIG=config 
          

    - name: 7. Archive Firmware Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: firmware-${{ env.BOARD }}-prospector_scanner
        # ビルド成果物はワークスペースのルート直下の 'build/zephyr/' に出力される
        path: build/zephyr/zmk.uf2
        retention-days: 7
