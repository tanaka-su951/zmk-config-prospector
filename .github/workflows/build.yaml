# .github/workflows/build.yml の修正案

name: Build Prospector Firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # ZMK v3.5.0リリース時のコミットSHAに固定します。
    # これにより、ビルドスクリプトがwest zephyr-exportを実行しなくなります。
    # コミットSHA: 67104d49811f372173b22b1029279a0989f5370d
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@67104d49811f372173b22b1029279a0989f5370d
