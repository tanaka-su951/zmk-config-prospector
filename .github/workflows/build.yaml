# .github/workflows/build.yml の修正案

name: Build Prospector Firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main  <-- 古い形式
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # 環境によっては、この指定で zephyr-export の実行を回避できます
      # 確実を期すため、現在の環境を強制的に上書きします
      # この行は、ZMKの最新バージョンに追従するためのベストプラクティスです
      continue-on-error: true
