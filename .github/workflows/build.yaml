# ZMK Custom Config Build Workflow (ZMK公式のセットアップスクリプトを使用)

# ZMK Custom Config Build Workflow (west zephyr-exportで環境を強制設定)

name: Build ZMK Firmware (Export Env Final)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout ZMK Config
      uses: actions/checkout@v4
      with:
        submodules: false
        fetch-depth: 0

    - name: Install Dependencies and Initialize ZMK Core
      run: |
        echo "--- 0. westとPython要件をインストール ---"
        pip install west
        
        echo "--- 1. ZMKコアをmainブランチでクローンし、west初期化 ---"
        rm -rf .west zmk # クリーンアップ
        git clone --depth 1 https://github.com/zmkfirmware/zmk zmk
        
        west init -l .
        west update
        
        echo "--- 2. ZMK Python要件をインストール ---"
        pip install -r zmk/requirements.txt || true

    - name: Install Zephyr SDK
      run: |
        echo "--- 3. Zephyr SDKのインストール ---"
        # SDK v0.16.0を使用
        SDK_VERSION="0.16.0"
        SDK_FILE="zephyr-sdk-${SDK_VERSION}_linux-x86_64.tar.xz"
        SDK_DIR="$HOME/zephyr-sdk"
        
        wget https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v${SDK_VERSION}/${SDK_FILE}
        mkdir -p $SDK_DIR
        tar xf ${SDK_FILE} -C $SDK_DIR --strip-components=1

    - name: --- 🚨 環境変数の設定とエクスポート 🚨 ---
      id: zephyr_env
      run: |
        echo "--- 4. Zephyr環境変数をエクスポート ---"
        # west zephyr-export を実行し、必要な環境変数を収集して次のステップに渡す
        # これが source setup.sh の代わりとなり、非対話モードで環境を設定する
        echo "ZEPHYR_TOOLCHAIN_VARIANT=zephyr" >> $GITHUB_ENV
        echo "ZEPHYR_SDK_INSTALL_DIR=$HOME/zephyr-sdk" >> $GITHUB_ENV
        echo "KCONFIG_NONINTERACTIVE=1" >> $GITHUB_ENV # Kconfig回避策
        
        # west zephyr-export の結果を環境変数としてエクスポート
        west zephyr-export >> $GITHUB_ENV

    - name: --- 🚨 ビルド実行 🚨 ---
      # west build実行時、前ステップで設定した環境変数が使用される
      run: |
        echo "--- Starting west build ---"
        west build -s zmk/app -d build --sysbuild \
          -b seeeduino_xiao_ble \
          -- -DSHIELD=prospector_scanner \
          -DZMK_CONFIG=$GITHUB_WORKSPACE \
          -DCONFIG_BUILD_WITH_CMAKE=n

    # ビルド後の成果物アップロードステップ
    - name: Archive artifacts
      uses: actions/upload-artifact@v4
      with:
        name: firmware
        path: build/zephyr/zmk.uf2
