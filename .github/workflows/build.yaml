# ZMK Custom Config Build Workflow (コミュニティアクションで環境構築)
# 🚨 これが、フリーズ/参照エラーを回避するための最後の最も実用的な構成です 🚨
... (中略)
    # 2. コミュニティアクションで環境を一括セットアップ
    - name: Install Dependencies and Toolchain
      # 安定した依存関係インストールアクションを使用
      uses: actions-toolkit/action-west@main
...
