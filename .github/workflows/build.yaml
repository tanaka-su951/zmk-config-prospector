# ZMK Custom Config Build Workflow (リリースアーカイブから直接ダウンロード)
# 🚨 これが、すべてのGit/WestエラーとSDKフリーズを回避する最終構成です 🚨

name: Build ZMK Firmware (Release Final)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    # 環境変数を定義して、ビルドのコマンドをシンプルにします
    env:
      BOARD: seeeduino_xiao_ble
      SHIELD: prospector_scanner
      ZMK_TAG: zmk-v3.5.0 # 安定版タグを指定

    steps:
    - name: Checkout ZMK Config (Your Repo)
      uses: actions/checkout@v4
      with:
        submodules: true
        fetch-depth: 0

    - name: Download and Extract ZMK Core Release
      run: |
        echo "--- 1. ZMKコアをリリースアーカイブから直接ダウンロード ---"
        # Gitコマンドを避け、安定版タグのZIPを直接ダウンロード
        ZMK_RELEASE_URL="https://github.com/zmkfirmware/zmk/archive/refs/tags/${{ env.ZMK_TAG }}.zip"
        curl -L $ZMK_RELEASE_URL -o zmk-core.zip
        
        # 展開し、フォルダ名を 'zmk' に変更
        unzip zmk-core.zip
        mv zmk-${{ env.ZMK_TAG }} zmk

    - name: Initialize West Workspace
      run: |
        echo "--- 2. west initとwest updateを実行 ---"
        # ダウンロードしたZMKコア内のwest.ymlをマニフェストとして指定
        west init -m zmk --mf west.yml
        
        # 依存関係をダウンロード
        west update
        
    - name: Install Python Dependencies
      run: |
        echo "--- 3. Python要件をインストール ---"
        pip install -r zmk/requirements.txt
        
    - name: Manually Setup Zephyr SDK
      run: |
        echo "--- 4. Zephyr SDKを手動でダウンロードして環境変数を設定 ---"
        # SDKのフリーズを回避するため、手動でダウンロード
        # ZMK v3.5.0 と互換性のあるSDKバージョン v0.16.0 を使用
        WEST_TOOLCHAIN_URL="https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v0.16.0/zephyr-sdk-0.16.0_linux-x86_64.tar.gz"
        WEST_TOOLCHAIN_DIR="$HOME/zephyr-sdk"
        
        mkdir -p $WEST_TOOLCHAIN_DIR
        curl -L $WEST_TOOLCHAIN_URL | tar -xz -C $WEST_TOOLCHAIN_DIR --strip-components=1
        
        # setup.shを実行せず、環境変数だけを設定（フリーズ回避）
        echo "ZEPHYR_TOOLCHAIN_VARIANT=zephyr" >> $GITHUB_ENV
        echo "ZEPHYR_SDK_INSTALL_DIR=$WEST_TOOLCHAIN_DIR" >> $GITHUB_ENV
        export KCONFIG_NONINTERACTIVE=1
        
    - name: Run West Build
      run: |
        echo "--- 5. west build実行 ---"
        # ツールチェーンと環境が設定された状態でビルドを実行
        west build -s zmk/app -d build --sysbuild \
          -b ${{ env.BOARD }} \
          -- -DSHIELD=${{ env.SHIELD }} \
          -DZMK_CONFIG=${{ github.workspace }}

    - name: Archive artifacts
      uses: actions/upload-artifact@v4
      with:
        name: firmware-${{ env.BOARD }}-${{ env.SHIELD }}
        path: build/zephyr/zmk.uf2
        retention-days: 7
