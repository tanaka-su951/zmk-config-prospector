# .github/workflows/build.yml の修正案

name: Build Prospector Firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # ZMKが推奨する、外部config用の安定版エイリアス（config-v3）に固定します。
    # これにより、参照エラーとwest zephyr-exportエラーの両方を回避できます。
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@config-v3
