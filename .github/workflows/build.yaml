name: Build ZMK Firmware using External Workflow

on:
  push:
    branches: [ main ]
    
  pull_request:
    branches: [ main ]
    
  workflow_dispatch:

jobs:
  build:
    # 外部ワークフローの呼び出し
    # 正しいファイル名: build-user-config.yml
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    
    # 外部ワークフローに渡す入力 (Inputs) を定義
    with:
    # ビルド対象のボードとシールドをJSON形式の配列で定義
      # build_targets の値全体をシングルクォートで囲み、
  # JSON文字列として正しく渡します。
  build_targets: '[{"board": "seeeduino_xiao_ble", "shield": "prospector_scanner"}]'
