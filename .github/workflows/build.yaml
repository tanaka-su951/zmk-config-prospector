# # ZMKの標準的な外部ビルドワークフローを使用
# このワークフローは、GitHub Actionsでの複雑なセットアップと
# 依存関係のクローン処理をすべてZMK公式側に任せるためのものです。

name: Build ZMK Firmware (Official Method)

on:
  push:
    branches: [ main ]
    
  pull_request:
    branches: [ main ]
    
  workflow_dispatch:

jobs:
  build:
    # ZMKの外部ワークフローを呼び出す
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # 最終修正: 入力値を YAML 配列 (リスト) の形式で強制的に渡します。
      # ZMKの外部ワークフローが単一の文字列ではなくリストを期待している問題を回避します。
      board: |
        - seeeduino_xiao_ble
      shield: |
        - prospector_scanner
      zmk_config_path: ${{ github.workspace }}
