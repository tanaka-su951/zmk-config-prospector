# ZMKビルドの最終安定版。ホストOSでチェックアウトを完了させてからDockerコンテナを使用します。

name: Build ZMK Firmware

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    # ワークフロー全体でデフォルトの作業ディレクトリを設定
    defaults:
      run:
        working-directory: ${{ github.workspace }}
        
    # ホストOSでチェックアウトを処理
    runs-on: ubuntu-latest
    
    # Dockerコンテナを使用
    container: zmkfirmware/zmk-build-arm:stable
    
    strategy:
      matrix:
        board: [seeeduino_xiao_ble]
        shield: [prospector_scanner]
        
    steps:
      # 1. リポジトリのチェックアウトとサブモジュールの取得
      #    path: ./config を指定することで、リポジトリを意図的にサブディレクトリにチェックアウトします。
      - name: Checkout repository and submodules
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          path: config # <-- 💥 重要な変更: リポジトリ全体を 'config' フォルダにチェックアウトします

      # 2. westワークスペースを明示的に初期化し、モジュールを更新
      #    west initはconfigディレクトリで行い、正しいパスにあるwest.ymlを参照させます。
      - name: Initialize and Update ZMK modules
        working-directory: config # <-- ステップ単位で作業ディレクトリを 'config' に変更
        run: |
          west init -l .
          west update --rebase
        
      # 3. ZMKファームウェアのビルド
      - name: Build ZMK firmware
        working-directory: config # <-- ステップ単位で作業ディレクトリを 'config' に変更
        run: west build -p -b ${{ matrix.board }} -- -DSHIELD=${{ matrix.shield }}

      # 4. ビルド成果物（ファームウェアファイル）をアーカイブ
      - name: Archive firmware
        uses: actions/upload-artifact@v4
        with:
          name: prospector_scanner-seeeduino_xiao_ble
          # ビルド結果が config/build/zephyr/zephyr.uf2 に生成されるためパスを変更
          path: config/build/zephyr/zephyr.uf2
