name: Build Prospector Firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # 外部ワークフローを安定版タグ v3.2.0 に固定します。
    # これにより、west zephyr-export のような廃止されたコマンドが実行されなくなります。
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v3.2.0
