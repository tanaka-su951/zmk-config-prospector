// config/prospector_scanner.overlay

// 🚨 修正: ZMKの標準的なDevicetreeインクルードパス形式に戻す
// DTC_INCLUDE_DIRSが正しく設定されているため、これで解決する可能性が高い
#include <devicetree/st7789v.dtsi> 
 
/ {
   chosen {
      // プライマリディスプレイとして st7789v を参照
      zephyr,display = &st7789v;
   };
};
