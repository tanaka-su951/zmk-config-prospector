/* config/prospector_scanner.overlay (最小構成 - ハードウェア設定なし) */

/ {
    chosen {
        // zmk,kscan = &kscan0; // キーマトリックスも無効化
    };

    // // すべてのカスタムノード定義を削除
    // kscan0: kscan { ... }; 
    // &spi0 { ... };
    // &pwm0 { ... }; 
};

&pwm0 {
    status = "okay";
    // Pin 10 を PWM チャンネルとして使用
    ch0-pin = <10>; 
};
