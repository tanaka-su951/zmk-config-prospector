#// 🚨 ST7789V ディスプレイのノード定義ファイルを、モジュールルートからの相対パスでインクルードします
// これにより、/chosen で参照されている &st7789v ノードが定義されます。
#include <devicetree/st7789v.dtsi>

// -----------------------------------------------------------
// 以下の内容は、以前のログから判明している /chosen ノードの内容です。
// 必要であれば、他のカスタムオーバーレイ内容をこの下に追記してください。
// -----------------------------------------------------------

/ {
   chosen {
      // Devicetreeの解析エラーの原因だった行
      zephyr,display = &st7789v; 
  };
};
